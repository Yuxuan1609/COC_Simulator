# S3-P2 实施计划：F5 疯狂 / F23 可重复实体 / F18+F10 时间效应 / F31 lint / F32 试玩报告

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 S3-P2 设计文档（`docs/superpowers/specs/2026-08-31-s3-p2-design.md`）的 4 项：F5 疯狂体系（轻方案）、F23 实体可重复策略、F18 时刻事件（降级版）+ F10 周期效应、F31 lint + F32 试玩报告。

**Architecture:** F5 以 `ScenarioWorld.on_san_loss` 统一钩子收拢三个 SAN 损失出口；F23 在 judge 硬挡点加 repeatable 放行；F18/F10 共用 `advance_time` 跨越检测；F31 扩 `cross_validate_layers`（管线内自动生效）+ 薄 CLI；F32 纯聚合无 rubric，独立阶段执行。

**Tech Stack:** Python 3.13, pytest。无新依赖。

**基线：** HEAD `505fcee`，默认套件 388 passed / 21 deselected。已知 flaky（复跑即过勿修）：`test_unresolved_use_becomes_creative`、`test_combat_phase_trigger`。

**通用约定：**
- TDD：每个 Task 先写失败测试再实现。
- 默认验证 `python -m pytest tests/ -q`；改了 prompt 的 Task（Task 2）完成后跑 `python -m pytest -m real_llm_smoke -q`。
- 改代码同步 MAINTENANCE.md（函数表行号 + changelog）；不提交无关脏文件（autosave、supplements、`imp.py`、`test.py`、`.claude/`）。
- AGENTS.md：不主动加注释（字段含义说明的行尾注释与现有一致风格可保留）。
- 测试构造模板参照 `tests/test_recovery.py`（`helpers.make_world`/`make_scene`，sys.path 双 insert）。

---

### Task 1: F5 核心——insanity 字段 + on_san_loss 钩子 + 三出口接线

**Files:**
- Modify: `src/investigator/models.py:201`（`__init__` 字段）
- Modify: `src/investigator/serialization.py:97,188-196`（to_dict/from_dict）
- Modify: `src/scenario_core.py`（`__init__` ~702 区、`on_san_loss` 新方法、apply_side_effects StatChange 分支 ~1445）
- Modify: `src/game/combat.py:147-161`（`_san_check_and_lose` 挂点注释更新）、`:803`、`:929-930`、`:1237`
- Modify: `src/game/judge.py:118-160`（施法 SAN 出口）
- Test: `tests/test_insanity.py`

设计要点（spec §1）：疯狂文本 LLM 生成在 Task 2 接线；本 Task 用注入式 `_insanity_llm`（缺省 None → 回退固定文案）。`san_lost_today` 采用**惰性跨日清零**（`on_san_loss` 内检测 `san_day != clock.day` 时重置），语义等价于 spec 的 advance_time 清零，且免改时间主路径。

- [ ] **Step 1: 写失败测试** `tests/test_insanity.py`

```python
"""F5 疯狂体系核心：on_san_loss 钩子 + insanity 字段入档（S3-P2 spec §1）。"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'tests', 'e2e'))


def _world_with_player(san=50, int_val=60):
    from helpers import make_world, make_scene
    from investigator import Investigator
    world = make_world({"room_a": make_scene()}, "room_a")
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.derived.SAN = san
    inv.stats.INT = int_val
    world.set_player(inv)
    return world, inv


def _force_roll(monkeypatch, value):
    monkeypatch.setattr("investigator.models.random.randint", lambda a, b: value)


class TestOnSanLoss:
    def test_accumulates_daily_loss(self):
        """on_san_loss 累计 san_lost_today 并记 san_day。"""
        world, inv = _world_with_player()
        world.on_san_loss(3, "markup")
        world.on_san_loss(2, "markup")
        assert inv.insanity["san_lost_today"] == 5
        assert inv.insanity["san_day"] == world.clock.day

    def test_lazy_reset_on_day_change(self):
        """跨日后首次损失：计数清零重记，san_at_day_start 取损失前 SAN。"""
        world, inv = _world_with_player(san=50)
        world.on_san_loss(2, "markup")
        world.advance_time(1440)
        world.on_san_loss(4, "markup")
        assert inv.insanity["san_lost_today"] == 4
        assert inv.insanity["san_at_day_start"] == 50  # 重置前的当前 SAN+本次损失

    def test_single_loss_ge5_triggers_temporary_on_int_fail(self, monkeypatch):
        """单次损失≥5 → INT 检定失败 → temporary 置位。"""
        world, inv = _world_with_player(int_val=60)
        _force_roll(monkeypatch, 100)  # INT 检定必败
        result = world.on_san_loss(5, "markup")
        assert result["temporary"] is True
        assert inv.insanity["temporary"]

    def test_single_loss_ge5_int_success_no_temporary(self, monkeypatch):
        """单次损失≥5 但 INT 检定成功 → 不触发临时疯狂。"""
        world, inv = _world_with_player(int_val=60)
        _force_roll(monkeypatch, 1)  # 大成功
        result = world.on_san_loss(5, "markup")
        assert result["temporary"] is False
        assert not inv.insanity.get("temporary")

    def test_temporary_not_retriggered(self, monkeypatch):
        """临时疯狂已置位 → 不再重复触发。"""
        world, inv = _world_with_player(int_val=60)
        _force_roll(monkeypatch, 100)
        world.on_san_loss(5, "markup")
        first_text = inv.insanity["temporary"]
        result = world.on_san_loss(6, "markup")
        assert result["temporary"] is False
        assert inv.insanity["temporary"] == first_text

    def test_cumulative_triggers_indefinite(self, monkeypatch):
        """当日累计 ≥ 起始SAN/5 → indefinite 置位。"""
        world, inv = _world_with_player(san=50)  # 阈值 50/5=10
        _force_roll(monkeypatch, 1)  # 屏蔽临时疯狂干扰
        result = world.on_san_loss(10, "markup")
        assert result["indefinite"] is True
        assert inv.insanity["indefinite"]

    def test_cumulative_below_threshold_no_indefinite(self, monkeypatch):
        world, inv = _world_with_player(san=50)
        _force_roll(monkeypatch, 1)
        result = world.on_san_loss(9, "markup")
        assert result["indefinite"] is False

    def test_zero_or_negative_loss_noop(self):
        world, inv = _world_with_player()
        result = world.on_san_loss(0, "markup")
        assert result == {"temporary": False, "indefinite": False}
        assert not inv.insanity

    def test_markup_san_loss_flows_to_hook(self, monkeypatch):
        """探索侧 @stat_change(SAN,-N) markup 经 apply_side_effects 汇入钩子。"""
        from game.side_effects import StatChange
        from scenario_core import apply_side_effects
        world, inv = _world_with_player(san=50)
        _force_roll(monkeypatch, 1)
        apply_side_effects(world, [StatChange(stat_name="SAN", delta=-3)])
        assert inv.derived.SAN == 47
        assert inv.insanity["san_lost_today"] == 3

    def test_insanity_serialization_roundtrip(self, tmp_path):
        """insanity 随角色卡序列化往返。"""
        from investigator.serialization import to_dict, from_dict
        world, inv = _world_with_player()
        inv.insanity = {"temporary": "幻觉丛生", "san_lost_today": 6, "san_day": 1}
        inv2 = from_dict(to_dict(inv))
        assert inv2.insanity["temporary"] == "幻觉丛生"
        assert inv2.insanity["san_lost_today"] == 6

    def test_insanity_default_missing_key(self):
        """旧卡无 insanity 键 → 默认空 dict。"""
        from investigator.serialization import to_dict, from_dict
        world, inv = _world_with_player()
        data = to_dict(inv)
        data.pop("insanity", None)
        inv2 = from_dict(data)
        assert inv2.insanity == {}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_insanity.py -q`
Expected: FAIL（`on_san_loss` 不存在 / `insanity` 属性不存在）

- [ ] **Step 3: 实现**

**`src/investigator/models.py`** `__init__` 在 `self.timed_effects`（:201）后加：

```python
        self.insanity: dict = {}              # F5 疯狂状态（S3-P2 spec §1）
```

**`src/investigator/serialization.py`**：
- `to_dict` 在 `"timed_effects"`（:97）后加一行：`"insanity": dict(getattr(inv, 'insanity', {}) or {}),`
- `from_dict` 在 `inv.timed_effects` 恢复块（:188-195）后加：`inv.insanity = dict(data.get("insanity", {}) or {})`

**`src/scenario_core.py`** `ScenarioWorld.__init__`（`san_seen_sources` :702 附近）加：

```python
        self._insanity_llm = None   # F5 疯狂文本生成器（Task 2 由 game_loop 注入）
```

新增两个方法（放在 `_apply_daily_recovery` 附近）：

```python
    def set_insanity_llm(self, llm_call):
        """注入疯狂文本生成器 callable(prompt)->str。None=回退固定文案。"""
        self._insanity_llm = llm_call

    def on_san_loss(self, loss: int, source: str = "") -> dict:
        """F5 疯狂判定钩子：所有 SAN 损失出口统一汇入（S3-P2 spec §1）。
        返回 {"temporary": bool, "indefinite": bool} 供调用方追加叙事。"""
        import logging
        result = {"temporary": False, "indefinite": False}
        p = self.player
        if p is None or loss <= 0:
            return result
        ins = p.insanity
        day = self.clock.day
        if ins.get("san_day") != day:  # 惰性跨日清零
            ins["san_lost_today"] = 0
            ins["san_day"] = day
            ins["san_at_day_start"] = p.derived.SAN + loss
        ins["san_lost_today"] = int(ins.get("san_lost_today", 0)) + loss
        if loss >= 5 and not ins.get("temporary"):
            ok, msg, tier = p.check_skill("INT")
            if not ok:
                ins["temporary"] = self._gen_insanity_text("临时疯狂", source)
                result["temporary"] = True
                logging.getLogger("scenario_core").info(
                    "[F5] 临时疯狂触发（单次损失 %d，%s）", loss, source)
        start = int(ins.get("san_at_day_start", 0) or 0)
        if (start and not ins.get("indefinite")
                and ins["san_lost_today"] >= max(1, start // 5)):
            ins["indefinite"] = self._gen_insanity_text("总结性疯狂", source)
            result["indefinite"] = True
            logging.getLogger("scenario_core").info(
                "[F5] 总结性疯狂触发（当日累计 %d/%d）", ins["san_lost_today"], start)
        return result

    def _gen_insanity_text(self, kind: str, source: str) -> str:
        """LLM 现场生成（Task 2 注入）；未注入时回退固定文案。"""
        if self._insanity_llm is None:
            return f"（{kind}）"
        try:
            p = self.player
            prompt = (f"调查员{p.name}因{source or '理智冲击'}陷入{kind}。"
                      f"用不超过 60 字中文描述其疯狂表现（如幻觉/偏执/歇斯底里），只写表现本身。")
            text = (self._insanity_llm(prompt) or "").strip()
            return text[:100] if text else f"（{kind}）"
        except Exception:
            return f"（{kind}）"
```

`apply_side_effects` 的 StatChange 分支（:1445-1447）改为：

```python
        elif isinstance(effect, StatChange):
            if world.player:
                before_san = world.player.derived.SAN
                new_val, detail = world.player.modify_stat(effect.stat_name, effect.delta)
                msgs.append(f"[属性变化] {detail}")
                if effect.stat_name.strip().upper() == "SAN":
                    loss = before_san - world.player.derived.SAN
                    if loss > 0:
                        trig = world.on_san_loss(loss, "事件冲击")
                        if trig["temporary"] or trig["indefinite"]:
                            msgs.append(f"[疯狂] {world.player.insanity.get('temporary') or world.player.insanity.get('indefinite')}")
```

**`src/game/combat.py`**：
- `_san_check_and_lose`（:155-157）log 文案更新为「F5 由调用方经 world.on_san_loss 处理」。
- 两个调用点（:803、:1237）在拿到 `loss, text` 后加：

```python
                if loss and self.world is not None:
                    trig = self.world.on_san_loss(loss, "战斗遭遇")
                    if trig["temporary"] or trig["indefinite"]:
                        text += "（疯狂侵袭）"
```

- 施法扣 SAN（:929-930）后加：

```python
            if need_san:
                player.derived.SAN = max(0, player.derived.SAN - need_san)
                if self.world is not None:
                    self.world.on_san_loss(need_san, "施法")
```

**`src/game/judge.py`** 施法出口（:159-160 refund 块之后）加：

```python
        if need_san and (success or not material.refund_on_fail):
            if getattr(self, "world", None) is not None:
                self.world.on_san_loss(need_san, "施法")
```

（即：refund 发生时 SAN 已退回，不计入疯狂累计。）

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_insanity.py -q`
Expected: 11 passed

- [ ] **Step 5: 全量回归 + MAINTENANCE + commit**

Run: `python -m pytest tests/ -q`
Expected: 399 passed / 21 deselected

MAINTENANCE.md：函数表加 `on_san_loss`/`set_insanity_llm` 行；changelog 加一行。

```bash
git add src/investigator/models.py src/investigator/serialization.py src/scenario_core.py src/game/combat.py src/game/judge.py tests/test_insanity.py MAINTENANCE.md
git commit -m "feat: F5 insanity core — on_san_loss hook, 3 SAN-loss outlets wired, persisted insanity field"
```

---

### Task 2: F5 表现层——LLM 文本注入 + prompt 注入

**Files:**
- Modify: `src/game_loop.py`（init_game 注入 insanity_llm）
- Modify: `src/investigator/models.py:304-320`（player.build_snapshot 加 insanity）
- Modify: `src/prompts.py:139-147`（`_build_investigator_info` 疯狂行）、keeper parse prompt 规则段（~:912-918）
- Test: `tests/test_insanity.py`（追加 3 个测试）

设计要点：`_build_investigator_info` 是 keeper parse（:471）与 narrator（:618）共用注入点，改这一处两边生效；检定提示词联动=keeper parse prompt 规则段加一条 bullet。

- [ ] **Step 1: 追加失败测试**（`tests/test_insanity.py` 末尾）

```python
class TestInsanityPresentation:
    def test_llm_generator_used_when_injected(self):
        """注入 llm 生成器 → 疯狂文本来自 LLM 输出。"""
        world, inv = _world_with_player(int_val=60)
        world.set_insanity_llm(lambda p: "他开始对着空气低语")
        import pytest
        monkeypatch = pytest.MonkeyPatch()
        monkeypatch.setattr("investigator.models.random.randint", lambda a, b: 100)
        world.on_san_loss(5, "markup")
        monkeypatch.undo()
        assert inv.insanity["temporary"] == "他开始对着空气低语"

    def test_snapshot_carries_insanity(self):
        """player.build_snapshot 在疯狂激活时携带 insanity。"""
        world, inv = _world_with_player()
        assert "insanity" not in inv.build_snapshot()
        inv.insanity = {"temporary": "幻觉丛生"}
        assert inv.build_snapshot()["insanity"]["temporary"] == "幻觉丛生"

    def test_investigator_info_renders_insanity(self):
        """_build_investigator_info 渲染疯狂状态行。"""
        from prompts import _build_investigator_info
        world, inv = _world_with_player()
        inv.insanity = {"temporary": "幻觉丛生", "phobia": "幽闭恐惧"}
        text = _build_investigator_info(world.build_snapshot())
        assert "疯狂状态" in text
        assert "幻觉丛生" in text
        assert "幽闭恐惧" in text
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_insanity.py::TestInsanityPresentation -q`
Expected: FAIL

- [ ] **Step 3: 实现**

**`src/game_loop.py`** `init_game`（world 创建并 set_player 之后）加：

```python
    def _insanity_llm(prompt: str) -> str:
        from llm import call_deepseek
        from config_llm import LLM_FLASH_MODEL
        return call_deepseek(
            prompt, json_mode=False, model=LLM_FLASH_MODEL,
            system="你是 COC 7th KP 助理，用简洁中文描写调查员的疯狂表现。")
    world.set_insanity_llm(_insanity_llm)
```

**`src/investigator/models.py`** `build_snapshot`（:304-320）返回 dict 构造改为：

```python
    def build_snapshot(self) -> dict:
        """Return a lightweight dict of player state for prompt contexts."""
        snap = {
            "name": self.name,
            ...（原有键不变）...
            "description": self.personal_description or "",
        }
        ins = getattr(self, "insanity", None) or {}
        active = {k: v for k, v in ins.items()
                  if k in ("temporary", "indefinite", "phobia", "mania") and v}
        if active:
            snap["insanity"] = active
        return snap
```

**`src/prompts.py`** `_build_investigator_info`（:139-147，`return` 前）加：

```python
    ins = p.get("insanity") or {}
    label_map = {"temporary": "临时疯狂", "indefinite": "总结性疯狂",
                 "phobia": "恐惧症", "mania": "躁狂症"}
    active = [f"{label_map[k]}={v}" for k, v in ins.items() if k in label_map and v]
    if active:
        parts.append("  疯狂状态：" + "；".join(active)
                     + "（叙事与检定演绎其影响，不机械复述）")
```

keeper parse prompt 规则段（difficulty 说明 :917 附近）加一行 bullet：

```
- 若调查员处于疯狂状态（见调查员信息），涉及疯狂/理智的检定与结果描述应体现其影响
```

- [ ] **Step 4: 测试 + real_llm_smoke + 回归 + commit**

Run: `python -m pytest tests/test_insanity.py -q` → 14 passed
Run: `python -m pytest tests/ -q` → 402 passed / 21 deselected
Run: `python -m pytest -m real_llm_smoke -q` → 4 passed（改了 prompt，必跑）

MAINTENANCE.md 行号刷新 + changelog。

```bash
git add src/game_loop.py src/investigator/models.py src/prompts.py tests/test_insanity.py MAINTENANCE.md
git commit -m "feat: F5 insanity presentation — LLM-generated madness text, prompt injection via investigator info"
```

---

### Task 3: F23 实体可重复策略

**Files:**
- Modify: `src/game/judge.py:269-276`（`_execute_entity` 门前判断）
- Modify: `src/prompts.py`（`_build_entity_lines` 完成段分类，~:400-462；`SHOW_COMPLETED` 呈现 :485）
- Modify: `src/module_designer/layered_schema.py`（L2 schema `repeatable` optional 字段）
- Test: `tests/test_repeatable.py`

设计要点（spec §2）：Entity 从 dict 来（L2 JSON），`repeatable` 经 `entity.extra` 或直接键读取——实现时先 grep `class Entity` 确认字段装载路径，以实际为准；语义=两档（缺省/False=once 保持现状）。completed 判定与 mark_completed 不变（幂等），只改「挡」与「藏」两处。

- [ ] **Step 1: 写失败测试** `tests/test_repeatable.py`

```python
"""F23 实体可重复策略（S3-P2 spec §2）。"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'tests', 'e2e'))


class TestRepeatable:
    def test_once_entity_blocked_after_completion(self):
        """默认（无 repeatable）实体完成后仍被硬挡——现状保持。"""
        # 构造 world + 一个 interaction 实体，执行两次，第二次返回"已触发过"
        ...

    def test_repeatable_entity_reruns(self):
        """repeatable=true 实体完成后可再次执行，runtime_state 仍幂等 mark_completed。"""
        ...

    def test_repeatable_not_hidden_in_completed_section(self):
        """repeatable 实体不进入 _build_entity_lines 的 completed 段。"""
        ...
```

（实现者参照 `tests/test_difficulty.py` / 既有 judge 测试的实体构造方式补全 `...` 处——先 grep tests 下 `_execute_entity` 的现有测试做模板。）

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_repeatable.py -q`
Expected: FAIL（repeatable 概念不存在）

- [ ] **Step 3: 实现**

- `judge.py` `_execute_entity` 门前（:271）：

```python
        if self._is_entity_completed(entity):
            repeatable = bool((entity.extra or {}).get("repeatable")
                              or getattr(entity, "repeatable", False))
            if not repeatable:
                return ActionOutcome(
                    intent=intent or ActionIntent(action="other"),
                    success=False, message="（该实体已触发过，无法重复执行）",
                    entity_id=entity.id, entity_type=entity.entity_type,
                )
```

（实体字段装载路径以实现时 grep `class Entity` 结果为准；若 Entity 是 dataclass 则在 dataclass 上加 `repeatable: bool = False` 并从 L2 dict 透传。）

- `prompts.py` `_build_entity_lines`：completed 分类处把 repeatable 实体从 completed_scene/completed_npc 排除（继续留在可触发段）。
- `layered_schema.py` L2 schema：interaction/auto_trigger/event 的字段规则加 `"repeatable": {"required": False}`。

- [ ] **Step 4: 测试 + 回归 + commit**

Run: `python -m pytest tests/test_repeatable.py -q` → 3 passed
Run: `python -m pytest tests/ -q`

```bash
git add src/game/judge.py src/prompts.py src/module_designer/layered_schema.py tests/test_repeatable.py MAINTENANCE.md
git commit -m "feat: F23 repeatable entity policy — once/repeatable, reruns allowed, not hidden after completion"
```

---

### Task 4: F18 时刻事件（降级版）

**Files:**
- Modify: `src/scenario_core.py`（`__init__` ~702 区、`advance_time` :755-771、`to_dict` :1191 区、`from_dict` :1222 区）
- Test: `tests/test_scheduled_events.py`

设计要点（spec §3）：payload=markup 文本；跨越检测 `old < at <= new`；触发后出队；触发的 markup 走 `parse_markup_all` + `apply_side_effects`。

- [ ] **Step 1: 写失败测试** `tests/test_scheduled_events.py`

```python
"""F18 时刻事件触发（降级版）：advance_time 跨越即触发（S3-P2 spec §3）。"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'tests', 'e2e'))


def _world():
    from helpers import make_world, make_scene
    from investigator import Investigator
    world = make_world({"room_a": make_scene()}, "room_a")
    world.set_player(Investigator(name="测试员", age=25, gender="男"))
    return world


class TestScheduledEvents:
    def test_crossing_triggers_markup(self):
        """advance_time 跨越 at_minutes → markup 结算（flag 置位）并出队。"""
        world = _world()
        world.scheduled_events.append({
            "id": "ev1", "at_minutes": 60,
            "markup": '@set_flag(flag="night_fall")', "description": "夜幕降临",
        })
        world.advance_time(30)
        assert world.get_runtime_state("flag:night_fall").completed is not True
        world.advance_time(60)  # 30 -> 90，跨越 60
        assert world.get_runtime_state("flag:night_fall").completed is True
        assert world.scheduled_events == []

    def test_not_reached_no_trigger(self):
        world = _world()
        world.scheduled_events.append({
            "id": "ev1", "at_minutes": 600,
            "markup": '@set_flag(flag="x")', "description": "",
        })
        world.advance_time(60)
        assert world.get_runtime_state("flag:x").completed is not True
        assert len(world.scheduled_events) == 1

    def test_multiple_events_in_one_advance(self):
        """一次推进跨越多事件 → 按时刻顺序全部触发。"""
        world = _world()
        for i, at in enumerate((60, 120, 180)):
            world.scheduled_events.append({
                "id": f"ev{i}", "at_minutes": at,
                "markup": f'@set_flag(flag="e{i}")', "description": "",
            })
        world.advance_time(200)
        for i in range(3):
            assert world.get_runtime_state(f"flag:e{i}").completed is True
        assert world.scheduled_events == []

    def test_save_load_roundtrip(self, tmp_path):
        """scheduled_events 入档往返。"""
        world = _world()
        world.scheduled_events.append({
            "id": "ev1", "at_minutes": 60, "markup": "", "description": "x"})
        world.save_state(str(tmp_path / "s.json"))
        from scenario_core import ScenarioWorld
        world2 = ScenarioWorld.load_state(str(tmp_path / "s.json"))
        assert world2.scheduled_events[0]["id"] == "ev1"
```

（markup 实际语法以 `game.side_effects` 现有 parser 为准——先 grep `@set_flag` 在测试/模组里的既有写法对齐；flag 读取方式同 `judge.py:523-541` 的 `flag:` 硬条件。）

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_scheduled_events.py -q`
Expected: FAIL（`scheduled_events` 属性不存在）

- [ ] **Step 3: 实现**

`src/scenario_core.py`：

`__init__`（`san_seen_sources` 附近）：

```python
        self.scheduled_events: list[dict] = []   # F18 时刻事件队列 [{id, at_minutes, markup, description}]（S3-P2 spec §3）
```

`advance_time`（:755-771）在 `_tick_time_effects` 前加：

```python
        old_time = self.clock.game_time
        ...
        self.clock.advance_time(minutes)
        ...
        self._fire_scheduled_events(old_time)
        self._tick_time_effects(minutes)
        self._apply_daily_recovery(old_day)
```

新方法：

```python
    def _fire_scheduled_events(self, old_time: int):
        """F18：跨越时刻的事件按序触发，payload=markup 走 apply_side_effects。"""
        import logging
        now = self.clock.game_time
        due = sorted(
            (e for e in self.scheduled_events
             if old_time < int(e.get("at_minutes", 0)) <= now),
            key=lambda e: int(e.get("at_minutes", 0)))
        if not due:
            return
        from game.side_effects import parse_markup_all
        for e in due:
            markup = str(e.get("markup", "") or "")
            logging.getLogger("scenario_core").info(
                "[F18] 时刻事件触发: %s @%s", e.get("id"), e.get("at_minutes"))
            if markup:
                effs = parse_markup_all(markup)
                if effs:
                    apply_side_effects(self, effs)
        due_ids = {id(e) for e in due}
        self.scheduled_events = [e for e in self.scheduled_events
                                 if id(e) not in due_ids]
```

`to_dict`（:1191 区）加 `"scheduled_events": [dict(e) for e in getattr(self, "scheduled_events", [])],`；`from_dict`（:1222 区）加 `world.scheduled_events = [dict(e) for e in data.get("scheduled_events", [])]`。

- [ ] **Step 4: 测试 + 回归 + commit**

Run: `python -m pytest tests/test_scheduled_events.py -q` → 4 passed
Run: `python -m pytest tests/ -q`（注意 F8 恢复回归）

```bash
git add src/scenario_core.py tests/test_scheduled_events.py MAINTENANCE.md
git commit -m "feat: F18 scheduled events — crossing detection in advance_time, markup payload via apply_side_effects"
```

---

### Task 5: F10 周期效应（interval + payload）

**Files:**
- Modify: `src/scenario_core.py`（`_tick_time_effects` :773-803）
- Modify: `src/game/combat.py:1246-1255`（`_tick_temporary_effects`）
- Test: `tests/test_periodic_effects.py`

设计要点（spec §4）：timed 条目扩 `interval`(round/hour/day) + `payload` 原子数组；缺省无 interval=现状纯计时。hour/day 按本次 advance 跨越次数结算；round 在战斗轮末。payload 结算写一个共享小函数（scenario_core 内），两侧调用——与 spec「双侧各自小循环」不冲突：结算逻辑共享、tick 点各自。

- [ ] **Step 1: 写失败测试** `tests/test_periodic_effects.py`

```python
"""F10 周期性/环境效应：timed 条目 interval+payload（S3-P2 spec §4）。"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'tests', 'e2e'))


def _world_with_player(hp=10):
    from helpers import make_world, make_scene
    from investigator import Investigator
    world = make_world({"room_a": make_scene()}, "room_a")
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.derived.HP = hp
    inv.derived.HP_MAX = 20
    inv.derived.SAN = 50
    world.set_player(inv)
    return world, inv


class TestPeriodicEffects:
    def test_no_interval_legacy_behavior(self):
        """无 interval 条目维持现状：只到期清除，不结算。"""
        world, inv = _world_with_player()
        inv.timed_effects.append({"id": "雾", "description": "浓雾",
                                  "expire_at": world.clock.game_time + 120})
        world.advance_time(60)
        assert inv.derived.HP == 10  # 无变化
        assert len(inv.timed_effects) == 1

    def test_hourly_payload_fires_per_crossing(self):
        """interval=hour + heal payload：推进 150 分钟 → 结算 2 次。"""
        world, inv = _world_with_player(hp=10)
        inv.timed_effects.append({
            "id": "疗养", "description": "", "interval": "hour",
            "expire_at": world.clock.game_time + 300,
            "payload": [{"type": "heal", "delta": 1}],
        })
        world.advance_time(150)
        assert inv.derived.HP == 12

    def test_daily_payload(self):
        """interval=day：跨 2 天 → 结算 2 次。"""
        world, inv = _world_with_player(hp=10)
        inv.timed_effects.append({
            "id": "诅咒", "description": "", "interval": "day",
            "expire_at": world.clock.game_time + 3000,
            "payload": [{"type": "heal", "delta": 1}],
        })
        world.advance_time(2880)
        assert inv.derived.HP == 12

    def test_payload_stops_after_expiry(self):
        """到期条目不再结算且被清除。"""
        world, inv = _world_with_player(hp=10)
        inv.timed_effects.append({
            "id": "毒", "description": "", "interval": "hour",
            "expire_at": world.clock.game_time + 60,
            "payload": [{"type": "heal", "delta": 1}],
        })
        world.advance_time(300)  # 300 分钟，但 60 分钟时到期 → 只结算 1 次
        assert inv.derived.HP == 11
        assert inv.timed_effects == []

    def test_san_payload_flows_to_insanity_hook(self):
        """payload 中 SAN 损失汇入 F5 钩子。"""
        world, inv = _world_with_player()
        inv.timed_effects.append({
            "id": "雾中恐惧", "description": "", "interval": "hour",
            "expire_at": world.clock.game_time + 300,
            "payload": [{"type": "markup", "text": "@stat_change(stat=SAN, delta=-2)"}],
        })
        world.advance_time(120)
        assert inv.insanity["san_lost_today"] == 4
```

（markup 语法以 `game.side_effects` 实际 parser 为准；payload 支持的原子子集 = heal / mp_change / markup / timed，同 spec §4。）

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_periodic_effects.py -q`
Expected: FAIL

- [ ] **Step 3: 实现**

`src/scenario_core.py` 新增 payload 结算函数（模块级，供战斗侧 import）：

```python
def apply_effect_payload(world, payload: list, source: str = "") -> list:
    """F10：结算 payload 原子子集（heal/mp_change/markup）。返回描述行。"""
    msgs = []
    p = world.player
    if p is None:
        return msgs
    for atom in payload or []:
        t = atom.get("type", "")
        if t == "heal":
            delta = max(0, int(atom.get("delta", 0) or 0))
            if delta:
                before = p.derived.HP
                p.derived.HP = min(p.derived.HP_MAX, p.derived.HP + delta)
                msgs.append(f"（{source}恢复 {p.derived.HP - before} 点 HP。）")
        elif t == "mp_change":
            delta = int(atom.get("delta", 0) or 0)
            if delta:
                p.derived.MP = max(0, min(p.derived.MP_MAX, p.derived.MP + delta))
        elif t == "markup":
            from game.side_effects import parse_markup_all
            effs = parse_markup_all(str(atom.get("text", "")))
            if effs:
                msgs.extend(apply_side_effects(world, effs))
    return msgs
```

`_tick_time_effects`（:773-803）在过期清除逻辑前加周期结算：

```python
        # F10 周期效应：hour/day interval 按跨越次数结算 payload
        now = self.clock.game_time
        old = now - max(0, minutes)
        for t in getattr(p, "timed_effects", []):
            interval = t.get("interval")
            if interval not in ("hour", "day"):
                continue
            span = 60 if interval == "hour" else 1440
            start_at = int(t.get("start_at", old))
            expire = int(t.get("expire_at", 0))
            fired = int(t.get("_fired", 0))
            total = max(0, (min(now, expire) - start_at) // span)
            if total > fired:
                apply_effect_payload(self, t.get("payload") or [],
                                     source=f"{t.get('id', '')}：")
                t["_fired"] = total
```

（`_fired`/`start_at` 是条目内运行时计数键，随 timed_effects 入档自然持久化；缺省 0/old 兼容旧条目。）

`src/game/combat.py` `_tick_temporary_effects`（:1246-1255）在 buff 递减后加：

```python
        # F10：round 周期效应（玩家 timed_effects 中 interval=round 条目）
        if self.world is not None and getattr(self.world, "player", None) is not None:
            from scenario_core import apply_effect_payload
            wp = self.world.player
            for t in getattr(wp, "timed_effects", []):
                if t.get("interval") == "round":
                    apply_effect_payload(self.world, t.get("payload") or [],
                                         source=f"{t.get('id', '')}：")
```

- [ ] **Step 4: 测试 + 回归 + commit**

Run: `python -m pytest tests/test_periodic_effects.py -q` → 5 passed
Run: `python -m pytest tests/ -q`（注意 F8/F18 回归互斥）

```bash
git add src/scenario_core.py src/game/combat.py tests/test_periodic_effects.py MAINTENANCE.md
git commit -m "feat: F10 periodic effects — interval+payload on timed_effects, hour/day via time tick, round via combat tick"
```

---

### Task 6: F31 lint（扩而不建 + 薄 CLI）

**Files:**
- Modify: `src/module_designer/layered_pipeline.py:97-219`（`cross_validate_layers` 扩编）
- Modify: `src/module_designer/dependency_graph.py:70-100`（`reachable_from`）
- Create: `src/module_designer/lint.py`
- Create: `src/module_designer/__main__.py`
- Test: `tests/test_module_lint.py`

- [ ] **Step 1: 写失败测试** `tests/test_module_lint.py`

```python
"""F31 模组体检 lint：cross_validate 扩编 + 可达性 + CLI（S3-P2 spec §5）。"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))


class TestReachability:
    def test_reachable_from_finds_unreachable(self):
        """reachable_from BFS：起点不可达的节点被报出。"""
        from module_designer.dependency_graph import DependencyGraph
        g = DependencyGraph()
        # 构造 A->B，C 孤立
        ...
        unreachable = g.reachable_from("A")
        assert "C" in unreachable and "B" not in unreachable

    def test_no_graph_returns_empty(self):
        ...


class TestLintChecks:
    def test_duplicate_entity_id_is_error(self):
        """跨场景重复 entity id → error。"""
        ...

    def test_unknown_markup_ref_is_error(self):
        """markup 内引用不存在的实体/flag → error。"""
        ...

    def test_cli_exit_code(self, tmp_path):
        """CLI：有 error → exit 1；干净模组 → exit 0。"""
        from module_designer.lint import run_lint
        ...
```

（构造参照 tests 下既有 layered_pipeline/cross_validate 测试的夹具——先 grep `cross_validate_layers` 在 tests 的用法对齐输入结构。）

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_module_lint.py -q`
Expected: FAIL

- [ ] **Step 3: 实现**

- `dependency_graph.py` 加 `reachable_from(start: str) -> list[str]`：BFS 沿 edges（source→target），返回**不在**可达集合内的节点 id 列表。
- `layered_pipeline.py` `cross_validate_layers` 追加检查：
  1. entity id 跨场景唯一性（收集 interactions+auto_triggers+events 的 id，重复→error）；
  2. markup 内引用：`side_effects` 字符串中正则提取 `@xxx(ref="...")`/`flag="..."`，校验实体/flag 存在（参照现有 :181-195 的 `@grant_spell` 检查写法）；
  3. npc_profiles 引用：L2 npc_profiles 的 scene 引用必须存在于 L1/L2 场景集；
  4. 结局引用：L3 ending_conditions 引用的 flag/实体存在。
  级别均 error（沿用 CrossRefReport.add）。
- `lint.py`：

```python
"""F31 模组体检 lint CLI（S3-P2 spec §5）。薄壳：加载三件套→复用管线验证函数。"""
import json, sys
from pathlib import Path


def run_lint(module_dir: str) -> int:
    """返回 exit code：有 error=1，否则 0。"""
    from module_designer.layered_schema import validate_all
    from module_designer.layered_pipeline import cross_validate_layers
    d = Path(module_dir)
    l1 = json.loads((d / "l1_player.json").read_text(encoding="utf-8"))
    l2 = json.loads((d / "l2_keeper.json").read_text(encoding="utf-8"))
    l3 = json.loads((d / "l3_designer.json").read_text(encoding="utf-8"))
    reports = validate_all(l1, l2, l3)
    cross = cross_validate_layers(l1, l2, l3)
    # 可达性 + 孤立场景（warning）
    # 难度分布（info）
    # 汇总打印 + 返回 1 if any error else 0
    ...


if __name__ == "__main__":
    sys.exit(run_lint(sys.argv[1]))
```

- `__main__.py`：`from module_designer.lint import run_lint; import sys; sys.exit(run_lint(sys.argv[1]))`

（`python -m module_designer` 与 `python -m module_designer.lint` 均可用。难度分布 info：遍历 L2 实体 `check.difficulty` 计数分档打印。）

- [ ] **Step 4: 测试 + 对一个真实模组跑 CLI 冒烟 + 回归 + commit**

Run: `python -m pytest tests/test_module_lint.py -q`
Run: `python -m module_designer.lint data/modules/e2e_testbed`（应 exit 0 或只报 info/warning）
Run: `python -m pytest tests/ -q`

```bash
git add src/module_designer/ tests/test_module_lint.py MAINTENANCE.md
git commit -m "feat: F31 module lint — extended cross_validate checks, reachability BFS, CLI entry"
```

---

### Task 7: F32 试玩报告（**独立阶段**——难度大，单列排期，可与其他批并行/延后）

> 用户拍板：F32 独立成阶段，需要大量测试。本 Task 可以最后做，也可以单独开一轮执行；不阻塞 Task 8 之前的游戏侧收口。

**Files:**
- Create: `src/module_designer/playtest_report.py`
- Modify: `src/llm_player.py:75-87`（goal 缺省读取 `module_meta.player_goal`）
- Modify: `src/module_designer/layered_schema.py`（L3 schema `player_goal` optional）
- Modify: 管线 L3 生成 prompt（一处，加 player_goal 字段说明——先 grep L3 prompt 所在文件）
- Test: `tests/test_playtest_report.py`

数据源事实（已核实）：`_summary.json` 的 `turns_detail` 每条含 `turn/input/skill_results/combat/location/ending/elapsed_s/time_state/mech`；summary 顶层含 `module/turns/total_elapsed_s/game_over/goal_achieved`。`skill_results` 条目 = `{entity_id, entity_type, tier, success, raw_check}`（game_loop.py:436-445）。模组侧总数从 L2（scenes/interactions/auto_triggers/events）+ L3（ending_conditions）读。

- [ ] **Step 1: 写失败测试** `tests/test_playtest_report.py`（全部用合成 `_summary.json` + 合成模组 dict，零 LLM 零 IO 外部依赖，tmp_path 落盘）

```python
"""F32 试玩报告：纯聚合无 rubric（S3-P2 spec §6）。"""
import json, os, sys

sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))


def _summary(turns):
    return {"module": "m", "player": "p", "turns": len(turns),
            "total_elapsed_s": 754.0, "game_over": None,
            "goal_achieved": False, "profile": {}, "turns_detail": turns}


def _turn(n, loc, **kw):
    base = {"turn": n, "input": "x", "brief": "", "narrative": "",
            "skill_results": [], "combat": None, "npc_events": [],
            "npcs_visible": {}, "pending": None, "location": loc,
            "weapons": [], "player_alive": True, "ending": None,
            "elapsed_s": 16.0, "time_state": {}, "time_agent": None,
            "mech": ""}
    base.update(kw)
    return base


class TestPlaytestReport:
    def test_scene_coverage(self):
        """场景覆盖率 = 到访场景去重 / 模组总场景。"""
        from module_designer.playtest_report import build_report
        turns = [_turn(1, "书房"), _turn(2, "书房"), _turn(3, "走廊")]
        module = {"scenes": {"书房": {}, "走廊": {}, "阁楼": {}, "地下室": {}}}
        rep = build_report(_summary(turns), module)
        assert rep["scene_coverage"]["visited"] == 2
        assert rep["scene_coverage"]["total"] == 4
        assert "阁楼" in rep["scene_coverage"]["missing"]

    def test_ending_reached_with_turn(self):
        turns = [_turn(1, "书房", ending="ending_escape")]
        ...

    def test_entity_trigger_rate_and_missing(self):
        """completed 实体 / 总实体；未触发清单。"""
        ...

    def test_check_difficulty_distribution(self):
        """skill_results × 模组 check.difficulty 交叉统计。"""
        ...

    def test_elapsed_and_turns(self):
        ...

    def test_markdown_output(self, tmp_path):
        """render_markdown 产出含四个指标段的报告文本。"""
        ...

    def test_empty_turns(self):
        """零回合不崩：各项为 0/空。"""
        ...

    def test_goal_fallback_to_player_goal(self):
        """llm_player profile 无 goal 时读 module_meta.player_goal。"""
        ...
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_playtest_report.py -q`
Expected: FAIL

- [ ] **Step 3: 实现 `playtest_report.py`**

```python
"""F32 模组试玩报告：单次试玩纯聚合，零 rubric 零 LLM（S3-P2 spec §6）。

输入：llm_player 的 _summary.json（turns_detail 全量 + mech 行）
输出：dict 报告 + render_markdown 文本
"""
import json


def build_report(summary: dict, module_l2: dict, module_l3: dict | None = None) -> dict:
    turns = summary.get("turns_detail", []) or []
    scenes = set((module_l2.get("scenes") or {}).keys())
    visited = []
    for t in turns:
        loc = t.get("location")
        if loc and loc not in visited:
            visited.append(loc)
    # 实体总数/触发
    entities = {}   # id -> difficulty
    for s, sd in (module_l2.get("scenes") or {}).items():
        for e in (sd.get("interactions") or []) + (sd.get("auto_triggers") or []) + (sd.get("events") or []):
            if isinstance(e, dict) and e.get("id"):
                entities[e["id"]] = ((e.get("check") or {}).get("difficulty") or "none")
    triggered = set()
    checks = []     # (entity_id, difficulty, tier, success)
    endings = []    # (name, turn)
    for t in turns:
        for sr in t.get("skill_results") or []:
            eid = sr.get("entity_id")
            if eid:
                triggered.add(eid)
                checks.append((eid, entities.get(eid, "unknown"),
                               sr.get("tier", ""), bool(sr.get("success"))))
        if t.get("ending"):
            endings.append((t["ending"], t.get("turn")))
    ...组装 dict（scene_coverage/endings/entity_trigger/check_distribution/elapsed）...


def render_markdown(report: dict) -> str:
    ...四指标段 + 耗时行，格式对齐 spec §6 示例...


def run_report(summary_path: str, module_dir: str, out_path: str | None = None) -> dict:
    """CLI 入口：读 _summary.json + 模组 l2/l3 → 报告 json（+ markdown）。"""
    ...
```

- [ ] **Step 4: goal 缺省读取**

`llm_player.py` `build_player_prompt`（:75-87）goal 模式取 goal 前：profile 无 `goal` 时尝试读模组 L3：

```python
    goal = profile.get("goal", "")
    if not goal:
        try:
            import json as _json
            from pathlib import Path as _P
            l3 = _json.loads((_P(__file__).resolve().parent.parent
                              / "data" / "modules"
                              / profile["player_config"]["module_name"]
                              / "l3_designer.json").read_text(encoding="utf-8"))
            meta = l3.get("module_meta") or {}
            goal = meta.get("player_goal") or str(l3.get("driving_force", ""))[:80]
        except Exception:
            goal = ""
```

`layered_schema.py` L3 schema `module_meta` 加 `"player_goal": {"required": False}`；管线 L3 生成 prompt 加一句字段说明（grep L3 prompt 定位）。

- [ ] **Step 5: 真实数据冒烟 + 回归 + commit**

用既有试玩日志（logs 下任一 `_summary.json`）+ 对应模组跑 `run_report`，人工核对输出合理。
Run: `python -m pytest tests/ -q`

```bash
git add src/module_designer/playtest_report.py src/llm_player.py src/module_designer/layered_schema.py tests/test_playtest_report.py MAINTENANCE.md
git commit -m "feat: F32 playtest report — pure aggregation (coverage/endings/checks/elapsed), player_goal fallback"
```

---

### Task 8: 收口（ISSUES / §10 同步 / MAINTENANCE / 全量回归）

- [ ] **Step 1: docs/ISSUES.md**：F5 / F23 / F18 / F10 / F31 / F32 从 §2 移入 §5 已收口（顶部，注明本批 commit）。F5 行注明「轻方案：状态标记+LLM 演绎，行为控制不做」。
- [ ] **Step 2: 簇评估 §10 清单同步**：按 spec §8 表回写——F5（san_loss 标注与疯狂频率）、F23（repeatable 默认）、F18（时刻事件声明格式）、F10（周期效应原子）均为暂无生产端；F31 无缺口；F32 的 player_goal 已落地、多次汇总等 F34。
- [ ] **Step 3: MAINTENANCE.md** 行号最终对齐（grep 实测）。
- [ ] **Step 4: 全量回归** `python -m pytest tests/ -q`；已知 flaky 复跑即过勿修。
- [ ] **Step 5: Commit**

```bash
git add docs/ISSUES.md docs/superpowers/specs/2026-08-31-cluster-assessment.md MAINTENANCE.md
git commit -m "docs: close S3-P2 (F5 insanity, F23 repeatable, F18 events, F10 periodic, F31 lint, F32 report)"
```

---

## Self-Review 记录

- **Spec 覆盖**：spec §1(F5)→Task1+2；§2(F23)→Task3；§3(F18)→Task4；§4(F10)→Task5；§5(F31)→Task6；§6(F32)→Task7；§7 测试约定→各 Task Step + Task 8；§8 管线影响→Task 8 Step 2。无缺口。
- **占位符扫描**：Task 3/6 留有 `...`，均附带「以现有测试为模板、先 grep 对齐」的明确指引——这两处依赖运行时才能确认的夹具结构（Entity 装载路径、cross_validate 测试输入），属有意为之的弹性点，不是偷懒。
- **类型一致性**：`on_san_loss` 返回 `{"temporary": bool, "indefinite": bool}` 在 Task 1 定义并在 combat/judge/测试一致使用；`insanity` 键集 {temporary, indefinite, phobia, mania, san_lost_today, san_day, san_at_day_start} 在序列化/snapshot/prompt 三处一致；`apply_effect_payload(world, payload, source)` 签名在定义与 combat 调用一致。
- **风险**：Task 4/5 都改 `advance_time` 主路径——Task 5 必须跑 F8/F18 回归；Task 2 改 prompt → real_llm_smoke 必跑。

---

## 执行记录（2026-09-01）

HEAD `2076a9c`。默认套件此前 **454 passed / 21 deselected**。

全量 `python -m pytest tests/ -m real_llm -q`（21 collected）：

| 轮次 | 结果 | 耗时 |
|---|---|---|
| 1 | **20 passed, 1 failed**（`tests/e2e/test_escalation_real.py::test_case_e`：Author 未新建场景，`Got: ['test_room', 'car_6']`） | 374.98s |
| 2（单条复跑 `test_case_e`） | **1 passed** | 155.78s |

判定：全量 real_llm **通过**（`test_case_e` 为既有 LLM 波动，复跑即过，非本批回归）。S1–S16 与 escalation A–D 首轮全绿。
