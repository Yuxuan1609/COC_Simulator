# S3-P3 实施计划：F17 scene_items + F19 environment

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 `docs/superpowers/specs/2026-09-02-s3-p3-design.md`：F17 平面 scene_items（hidden/exposed、免费拾取/丢弃、废弃 offer）+ F19 环境两轴修正（game_config 数值表 + `@env_change`）。

**Architecture:** `SceneItem` 为运行时唯一事实源；旧 `scene_weapons` 加载/入档映射合并。拾取/丢弃走 understand 短路（零 LLM、不推进 `turn_number`/时间）。环境修正在 `check_skill` 目标值上 ±N，不改难度档。

**Tech Stack:** Python 3.13, pytest。无新依赖。

**基线：** HEAD `4bcc418`。默认套件 `pytest tests/ -q`。已知 flaky（复跑即过勿修）：`test_unresolved_use_becomes_creative`、`test_combat_phase_trigger`。

**通用约定：**
- TDD：每个 Task 先写失败测试再实现。
- 默认验证 `python -m pytest tests/ -q`；改了 prompt 的 Task 完成后跑 `python -m pytest -m real_llm_smoke -q`。
- 改代码同步 MAINTENANCE.md；不提交无关脏文件。
- AGENTS.md：不主动加注释。
- 测试构造模板参照 `tests/test_recovery.py`（`helpers.make_world`/`make_scene`，sys.path 双 insert）。

**文件地图：**
- `src/game/side_effects.py` — `SceneItem` dataclass
- `src/scenario_core.py` — Node/world 加载入档、environment
- `src/game/agents/keeper.py` — 拾取/丢弃短路，offer 删除
- `src/game/turn/understand.py` / `adjudicate.py` / `finalize.py` — offer 拆除、搜索暴露
- `src/investigator/models.py` + `rules.py` + `data/game_config.json` — 环境修正
- `src/game/judge.py` — 检定应用 env_mod
- `src/prompts.py` — exposed 物品 + 环境知情行
- 测试：`tests/test_scene_items.py`、`tests/test_environment.py`、改写 `tests/e2e/test_deterministic.py` offer 用例

---

### Task 1: SceneItem 数据模型 + 加载映射 + 入档往返

**Files:**
- Modify: `src/game/side_effects.py`
- Modify: `src/scenario_core.py`（Node @264、`__init__` @692-713、`build_snapshot` @1194、`to_dict` @1284、`from_dict` @1404、DirectedGraph 序列化 @346/@441）
- Modify: `src/module_designer/layered_schema.py`（L2_SCENE_SCHEMA 加 `scene_items` 可选；`scene_weapons` 保留）
- Test: `tests/test_scene_items.py`

- [ ] **Step 1: 写失败测试** `tests/test_scene_items.py`

```python
"""F17 scene_items：加载映射 + 入档往返（S3-P3 spec §1.1）。"""
import os, sys
sys.path.insert(0, os.path.join(os.path.dirname(__file__), '..', 'src'))
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'e2e'))


def test_scene_weapons_map_to_scene_items_exposed():
    from helpers import make_world, make_scene
    from game.side_effects import SceneWeapon
    world = make_world({"room_a": make_scene()}, "room_a")
    world.scene_weapons["room_a"] = [
        SceneWeapon(weapon_ref="手枪", scene="room_a", quantity=1)]
    world._hydrate_scene_items_from_weapons()
    items = world.scene_items.get("room_a", [])
    assert len(items) == 1
    assert items[0].kind == "weapon" and items[0].ref == "手枪"
    assert items[0].hidden is False
    assert not hasattr(items[0], "quantity") or items[0].quantity == 1


def test_node_scene_items_load_and_save_roundtrip():
    from scenario_core import DirectedGraph, ScenarioWorld
    graph = DirectedGraph({
        "room_a": {
            "from_here": [], "to_here": [], "interactions": [],
            "auto_triggers": [], "encounters": [], "scene_weapons": [],
            "scene_items": [
                {"kind": "item", "ref": "钥匙", "quantity": 1, "hidden": True},
                {"kind": "weapon", "ref": "手枪", "hidden": False},
            ],
        }
    })
    world = ScenarioWorld(graph, "room_a")
    items = world.scene_items["room_a"]
    assert {(i.kind, i.ref, i.hidden) for i in items} == {
        ("item", "钥匙", True), ("weapon", "手枪", False)}
    dumped = world.to_dict()
    world2 = ScenarioWorld.from_dict(dumped, graph)
    items2 = world2.scene_items["room_a"]
    assert {(i.kind, i.ref, i.hidden) for i in items2} == {
        ("item", "钥匙", True), ("weapon", "手枪", False)}


def test_legacy_save_scene_weapons_hydrate_on_load():
    """旧档只有 scene_weapons 键：加载后并入 scene_items（hidden=false）。"""
    from helpers import make_world, make_scene
    world = make_world({"room_a": make_scene()}, "room_a")
    data = world.to_dict()
    data.pop("scene_items", None)
    data["scene_weapons"] = {"room_a": [{"weapon_ref": "手枪", "quantity": 1}]}
    world2 = type(world).from_dict(data, world.graph)
    items = world2.scene_items.get("room_a", [])
    assert any(i.kind == "weapon" and i.ref == "手枪" and not i.hidden for i in items)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_scene_items.py -q`
Expected: FAIL（`SceneItem` / `scene_items` / `_hydrate_scene_items_from_weapons` 不存在）

- [ ] **Step 3: 最小实现**

`src/game/side_effects.py` 在 `SceneWeapon` 旁加：

```python
@dataclass
class SceneItem:
    kind: str          # "weapon" | "item"
    ref: str
    hidden: bool = False
    quantity: int = 1  # 武器恒 1，物品才有意义
```

`ScenarioWorld.__init__`：加 `self.scene_items: dict[str, list] = {}`，加载 node.scene_items；再调 `_hydrate_scene_items_from_weapons()` 把 `node.scene_weapons` / 已有 `self.scene_weapons` 映射进去（同 ref 不重复）。

```python
def _hydrate_scene_items_from_weapons(self) -> None:
    for scene, weps in self.scene_weapons.items():
        existing = {(i.kind, i.ref) for i in self.scene_items.get(scene, [])}
        bucket = self.scene_items.setdefault(scene, [])
        for sw in weps:
            key = ("weapon", sw.weapon_ref)
            if key in existing:
                continue
            bucket.append(SceneItem(kind="weapon", ref=sw.weapon_ref, hidden=False, quantity=1))
            existing.add(key)
```

Node 加 `scene_items: list = field(default_factory=list)`；DirectedGraph 加载/to_dict 透传。world.to_dict 写 `scene_items`（kind/ref/hidden/quantity）；from_dict 读 `scene_items`，若缺则从 `scene_weapons` hydrate。`scene_weapons` 键保留写入（由 scene_items 中 kind=weapon 投影），旧测试暂时不炸——Task 2 改测试后再停投影。

本 Task **仍同步维护 `world.scene_weapons`**（投影），避免半截状态。Task 2 把读写切到 scene_items 后投影可改为只读兼容或删除（以测试为准）。

layered_schema `L2_SCENE_SCHEMA` 加 `"scene_items": {"required": False}`。

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_scene_items.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/game/side_effects.py src/scenario_core.py src/module_designer/layered_schema.py tests/test_scene_items.py MAINTENANCE.md
git commit -m "feat: F17 SceneItem model + scene_weapons hydrate + save/load"
```

---

### Task 2: 搜索暴露 + 免费拾取 + 废弃 offer

**Files:**
- Modify: `src/game/agents/keeper.py`（`_detect_direct_pickup` / `_grant_scene_weapons` / `_weapon_offer`）
- Modify: `src/game/turn/understand.py:19-50`
- Modify: `src/game/turn/adjudicate.py:140-150`
- Modify: `src/game/turn/finalize.py:91-117`
- Modify: `tests/e2e/test_deterministic.py`（`TestOfferAnswerLoop` + `TestWeaponPickupRules` offer 三测改写/删除）
- Test: `tests/test_scene_items.py` 追加

行为（spec §1.2-1.5）：
- 搜索**成功**：当前场景全部 `hidden=True` → `False`，叙事列出新暴露名；不弹是/否。
- 搜索失败：不暴露。
- 拾取：understand 在 `turn_number += 1` **之前**短路（现 R1 位置），只捡 **exposed** 且未持有。武器走武器槽；物品走 ItemManager。hidden 点名 → 「你没发现这东西」，同样 Early、不推进回合。
- `_weapon_offer` / `_weapon_offer_msg` / finalize pending `weapon_offer` / understand 是/否应答 **整段删除**。
- keeper.py:421 附近「获得了武器是否接受」若仍走 offer，改为直接授予或改为 exposed 条目（无阻塞）。检索 `_weapon_offer` 全库清零。

- [ ] **Step 1: 写失败测试**（追加到 `tests/test_scene_items.py`）+ 改写 e2e

```python
class TestSearchExpose:
    def test_search_success_exposes_hidden(self, monkeypatch):
        from helpers import make_world, make_scene
        from game.side_effects import SceneItem
        from game.agents.keeper import Keeper
        from game_loop import run_turn
        from e2e.helpers import stub_keeper_llm, make_game, assert_player_turn_contract
        from investigator import Investigator
        world = make_world({"room_a": make_scene()}, "room_a")
        world.set_player(Investigator(name="测试员", age=25, gender="男"))
        world.scene_items["room_a"] = [
            SceneItem(kind="item", ref="钥匙", hidden=True, quantity=1)]
        keeper = Keeper(world)
        stub_keeper_llm(keeper, monkeypatch,
                        parse_results=[[{"type": "search", "text": "搜索"}]])
        monkeypatch.setattr("investigator.models.random.randint", lambda a, b: 1)
        game = make_game(keeper)
        r = run_turn(game, "搜索四周")
        assert_player_turn_contract(r)
        assert r.pending_interaction is None, "不得再弹 weapon_offer"
        assert world.scene_items["room_a"][0].hidden is False
        assert "钥匙" in r.narrative

    def test_search_fail_does_not_expose(self, monkeypatch):
        from helpers import make_world, make_scene, stub_keeper_llm, make_game
        from game.side_effects import SceneItem
        from game.agents.keeper import Keeper
        from game_loop import run_turn
        from investigator import Investigator
        world = make_world({"room_a": make_scene()}, "room_a")
        world.set_player(Investigator(name="测试员", age=25, gender="男"))
        world.scene_items["room_a"] = [
            SceneItem(kind="item", ref="钥匙", hidden=True, quantity=1)]
        keeper = Keeper(world)
        stub_keeper_llm(keeper, monkeypatch,
                        parse_results=[[{"type": "search", "text": "搜索"}]])
        monkeypatch.setattr("investigator.models.random.randint", lambda a, b: 100)
        r = run_turn(make_game(keeper), "搜索四周")
        assert r.pending_interaction is None
        assert world.scene_items["room_a"][0].hidden is True

    def test_pickup_exposed_item_free(self, monkeypatch):
        from helpers import make_world, make_scene, stub_keeper_llm, make_game
        from game.side_effects import SceneItem
        from game.agents.keeper import Keeper
        from game_loop import run_turn
        from investigator import Investigator
        world = make_world({"room_a": make_scene()}, "room_a")
        inv = Investigator(name="测试员", age=25, gender="男")
        world.set_player(inv)
        world.scene_items["room_a"] = [
            SceneItem(kind="item", ref="钥匙", hidden=False, quantity=1)]
        keeper = Keeper(world)
        stub_keeper_llm(keeper, monkeypatch, parse_results=[[{"type": "other"}]])
        r = run_turn(make_game(keeper), "我捡起钥匙")
        assert r.pending_interaction is None
        assert inv.item_manager.has("钥匙")
        assert not world.scene_items.get("room_a")

    def test_pickup_hidden_rejected(self, monkeypatch):
        from helpers import make_world, make_scene, stub_keeper_llm, make_game
        from game.side_effects import SceneItem
        from game.agents.keeper import Keeper
        from game_loop import run_turn
        from investigator import Investigator
        world = make_world({"room_a": make_scene()}, "room_a")
        inv = Investigator(name="测试员", age=25, gender="男")
        world.set_player(inv)
        world.scene_items["room_a"] = [
            SceneItem(kind="item", ref="钥匙", hidden=True, quantity=1)]
        keeper = Keeper(world)
        stub_keeper_llm(keeper, monkeypatch, parse_results=[[{"type": "other"}]])
        r = run_turn(make_game(keeper), "我捡起钥匙")
        assert not inv.item_manager.has("钥匙")
        assert "没发现" in r.narrative
        assert world.scene_items["room_a"][0].hidden is True

    def test_pickup_weapon_still_works(self, monkeypatch):
        from helpers import make_world, make_scene, stub_keeper_llm, make_game
        from game.side_effects import SceneItem
        from game.agents.keeper import Keeper
        from game_loop import run_turn
        from investigator import Investigator
        from library.weapons import WeaponLibrary, LibraryWeapon
        wlib = WeaponLibrary()
        wlib._weapons["手枪"] = LibraryWeapon(name="手枪", skill_name="射击(手枪)")
        world = make_world({"room_a": make_scene()}, "room_a", weapon_library=wlib)
        inv = Investigator(name="测试员", age=25, gender="男")
        world.set_player(inv)
        world.scene_items["room_a"] = [
            SceneItem(kind="weapon", ref="手枪", hidden=False)]
        keeper = Keeper(world)
        stub_keeper_llm(keeper, monkeypatch, parse_results=[[{"type": "other"}]])
        r = run_turn(make_game(keeper), "我捡起手枪")
        assert any(w.name == "手枪" for w in inv.weapons)
        assert not world.scene_items.get("room_a")
```

e2e `TestOfferAnswerLoop.test_search_offer_then_pickup`：**改写**为「搜索成功暴露 + 下回合捡起」（无 pending）。`test_offer_gate_*` 三测 **删除**（机制不存在）。`test_direct_pickup_*` 保留，断言改为读 `scene_items`（或投影仍在则双断言）。

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_scene_items.py tests/e2e/test_deterministic.py::TestOfferAnswerLoop tests/e2e/test_deterministic.py::TestWeaponPickupRules -q`
Expected: 新测 FAIL；offer 旧测 FAIL（pending 不再出现）

- [ ] **Step 3: 实现**

keeper：
```python
def _detect_direct_pickup(self, raw: str) -> dict | None:
    """返回 {kind, ref} 或 None。只匹配当前场景 exposed 条目。"""
    ...

def _grant_scene_item(self, kind: str, ref: str) -> str:
    """武器→add_weapon 并从 scene_items 移除；物品→item_manager.add 并减 quantity/移除。"""
    ...
```

understand 拾取短路（`turn_number += 1` 之前）：
- hidden 命中名字 → Early「你没发现这东西。」
- exposed 命中 → `_grant_scene_item` → Early「你拾起了X。」
删除 offer 是/否块。

adjudicate 搜索成功分支：遍历 `world.scene_items[loc]`，`hidden=True` 改 False，拼「你发现了 …」；**不要**设 `_weapon_offer`。失败分支不改 hidden。

finalize：删除 offer_pending / `_weapon_offer_msg` 注入。

`grep -n _weapon_offer src tests` 必须归零（测试改写后）。

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_scene_items.py tests/e2e/test_deterministic.py::TestWeaponPickupRules tests/e2e/test_deterministic.py::TestOfferAnswerLoop tests/test_turn_result_contract.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git commit -m "feat: F17 search-expose + free pickup; remove weapon_offer loop"
```

---

### Task 3: 丢弃到场景

**Files:**
- Modify: `src/game/agents/keeper.py`（`_detect_direct_drop` / `_drop_to_scene`）
- Modify: `src/game/turn/understand.py`（拾取短路旁加丢弃短路，同样在 `turn_number += 1` 之前）
- Test: `tests/test_scene_items.py`

- [ ] **Step 1: 写失败测试**

```python
def test_drop_item_to_scene_exposed(self, monkeypatch):
    from helpers import make_world, make_scene, stub_keeper_llm, make_game
    from game.agents.keeper import Keeper
    from game_loop import run_turn
    from investigator import Investigator
    world = make_world({"room_a": make_scene()}, "room_a")
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.item_manager.add("钥匙", quantity=1)
    world.set_player(inv)
    keeper = Keeper(world)
    stub_keeper_llm(keeper, monkeypatch, parse_results=[[{"type": "other"}]])
    r = run_turn(make_game(keeper), "我丢掉钥匙")
    assert not inv.item_manager.has("钥匙")
    items = world.scene_items.get("room_a", [])
    assert any(i.kind == "item" and i.ref == "钥匙" and not i.hidden for i in items)

def test_drop_weapon_to_scene(self, monkeypatch):
    from helpers import make_world, make_scene, stub_keeper_llm, make_game
    from game.agents.keeper import Keeper
    from game_loop import run_turn
    from investigator import Investigator
    from library.weapons import WeaponLibrary, LibraryWeapon
    from investigator.models import Weapon
    wlib = WeaponLibrary()
    wlib._weapons["手枪"] = LibraryWeapon(name="手枪", skill_name="射击(手枪)")
    world = make_world({"room_a": make_scene()}, "room_a", weapon_library=wlib)
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.add_weapon(Weapon(name="手枪", skill_name="射击(手枪)"))
    world.set_player(inv)
    keeper = Keeper(world)
    stub_keeper_llm(keeper, monkeypatch, parse_results=[[{"type": "other"}]])
    run_turn(make_game(keeper), "我丢掉手枪")
    assert not any(w.name == "手枪" for w in inv.weapons)
    items = world.scene_items.get("room_a", [])
    assert any(i.kind == "weapon" and i.ref == "手枪" and not i.hidden for i in items)

def test_drop_then_pickup_roundtrip(self, monkeypatch):
    from helpers import make_world, make_scene, stub_keeper_llm, make_game
    from game.agents.keeper import Keeper
    from game_loop import run_turn
    from investigator import Investigator
    world = make_world({"room_a": make_scene()}, "room_a")
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.item_manager.add("钥匙", quantity=1)
    world.set_player(inv)
    keeper = Keeper(world)
    stub_keeper_llm(keeper, monkeypatch, parse_results=[[{"type": "other"}]])
    game = make_game(keeper)
    run_turn(game, "我丢掉钥匙")
    run_turn(game, "我捡起钥匙")
    assert inv.item_manager.has("钥匙")
    assert not world.scene_items.get("room_a")
```

动词：丢/扔/放下/丢掉。否定词不触发。未持有 → Early「你没有X。」

- [ ] **Step 2: 跑测试确认失败**
- [ ] **Step 3: 实现短路**（不推进回合/时间，镜像拾取）
- [ ] **Step 4: 跑测试确认通过**
- [ ] **Step 5: Commit** `feat: F17 drop to scene as exposed SceneItem`

---

### Task 4: snapshot / prompt 呈现 exposed 物品

**Files:**
- Modify: `src/scenario_core.py` `build_snapshot` @1194（`scene_weapons` 改为 exposed `scene_items` 列表，含 kind/ref；可保留 `scene_weapons` 键为武器投影以免前端炸）
- Modify: `src/prompts.py` `_build_scene_state` @181-183：列出 **exposed** 物品+武器（hidden 不进 prompt）
- Test: `tests/test_scene_items.py` 断言 snapshot/prompt 不含 hidden、含 exposed

改 prompt → 本 Task 结束后跑 `python -m pytest -m real_llm_smoke -q`。

- [ ] **Step 5: Commit** `feat: F17 snapshot/prompt list exposed scene_items`

---

### Task 5: F19 environment 字段 + 修正表 + check_skill ±N

**Files:**
- Modify: `data/game_config.json`、`src/investigator/rules.py` `_GAME_CONFIG_DEFAULTS`
- Modify: `src/scenario_core.py` Node 加 `environment: dict`（默认 `{}`）；world 提供 `current_environment() -> dict`（当前场景节点字段，可被运行时覆盖——Task 6 写入 `world.environment_overrides` 或直接改 node.extra/node.environment）
- Modify: `src/investigator/models.py` `_roll_d100`：`target` 先 `clamp(1,99, target + modifier)`
- Modify: `src/investigator/models.py` `check_skill(..., modifier: int = 0)` 传入 `_roll_d100`
- Modify: `src/game/judge.py:150,361`、`src/game/turn/adjudicate.py:106`、`src/game/agents/keeper.py:275`：调用前取 `env_check_modifier(world, skill_name)`
- 不改 `scenario_core.py` 疯狂 INT 检定（@891）
- Modify: `src/module_designer/layered_schema.py` L2_SCENE_SCHEMA 加 `environment`
- Test: `tests/test_environment.py`

修正表初值（spec 示例，可原样进 config）：

```json
"env_check_modifiers": {
  "dark":  {"侦查": -20, "潜行": 10},
  "dim":   {"侦查": -10},
  "noisy": {"聆听": -20}
}
```

缺省 lighting=normal、noise=quiet → 修正 0。多轴同技能求和。

```python
def env_check_modifier(world, skill_name: str) -> int:
    env = world.current_environment()
    table = get_game_config()["env_check_modifiers"]
    total = 0
    for axis in ("lighting", "noise"):
        tag = env.get(axis) or {"lighting": "normal", "noise": "quiet"}[axis]
        total += int(table.get(tag, {}).get(skill_name, 0) or 0)
    return total
```

- [ ] **Step 1: 写失败测试**

```python
def test_dark_spot_hidden_applies_minus_20(monkeypatch):
    """侦查 50 + dark -20 → 有效目标 30；roll=40 失败。"""
    from investigator import Investigator
    from investigator.models import Skill
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.skills.append(Skill(name="侦查", base_value=50, value=50, category="探索"))
    monkeypatch.setattr("investigator.models.random.randint", lambda a, b: 40)
    ok, msg, tier = inv.check_skill("侦查", modifier=-20)
    assert not ok
    assert "/30" in msg or "30" in msg

def test_no_environment_zero_mod(monkeypatch):
    from investigator import Investigator
    from investigator.models import Skill
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.skills.append(Skill(name="侦查", base_value=50, value=50, category="探索"))
    monkeypatch.setattr("investigator.models.random.randint", lambda a, b: 40)
    ok, msg, tier = inv.check_skill("侦查", modifier=0)
    assert ok

def test_explicit_difficulty_still_applied_plus_env(monkeypatch):
    """hard + modifier=-20：effective=30，hard 阈值 15；roll=20 失败。"""
    from investigator import Investigator
    from investigator.models import Skill
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.skills.append(Skill(name="侦查", base_value=50, value=50, category="探索"))
    monkeypatch.setattr("investigator.models.random.randint", lambda a, b: 20)
    ok, msg, tier = inv.check_skill("侦查", difficulty="hard", modifier=-20)
    assert not ok

def test_clamp_1_99(monkeypatch):
    from investigator import Investigator
    from investigator.models import Skill
    inv = Investigator(name="测试员", age=25, gender="男")
    inv.skills.append(Skill(name="侦查", base_value=5, value=5, category="探索"))
    monkeypatch.setattr("investigator.models.random.randint", lambda a, b: 1)
    ok, msg, tier = inv.check_skill("侦查", modifier=-20)
    assert ok  # clamp 到 1，roll=1 大成功
```

叠加语义锁定：**modifier 加在技能值（target）上，再按 difficulty 算 hard/extreme 阈值**（`effective = clamp(1,99, skill_value + env_mod)` 然后 hard=effective//2）。

- [ ] **Step 2-4: RED → 实现 → GREEN**
- [ ] **Step 5: Commit** `feat: F19 env tags + skill-value modifiers from game_config`

---

### Task 6: @env_change markup

**Files:**
- Modify: `src/game/side_effects.py`（`EnvChange` dataclass + `_MARKUP_PATTERN` 加 `env_change` + `_build_side_effect`）
- Modify: `src/scenario_core.py` `apply_side_effects` 分支；`_effect_to_dict` 若有
- Modify: `src/game/judge.py` `_MARKUP_STRIP_RE`、`src/prompts.py` `_STRIP_MARKUP_RE` 同步加 `env_change`
- Test: `tests/test_environment.py`

语法：`@env_change(axis="lighting", value="normal")`
非法 axis/value → 忽略 + `logging.warning`，不抛。
合法值：lighting ∈ {dark,dim,normal}；noise ∈ {quiet,noisy}。

运行时覆盖存在 `world.environment_overrides: dict[str, dict]`（scene → {axis: value}），`current_environment()` = node.environment + override。入档 `to_dict`/`from_dict`。

- [ ] **Step 5: Commit** `feat: F19 @env_change markup + persist overrides`

---

### Task 7: 环境知情行进 prompt

**Files:**
- Modify: `src/prompts.py` `_build_scene_state` 或 `_build_time_block` 旁加「环境：黑暗/嘈杂」（无标签不输出）
- Modify: `src/scenario_core.py` `build_snapshot` 加 `environment`
- Test: 快照/prompt 字符串断言
- 跑 `python -m pytest -m real_llm_smoke -q`

- [ ] **Step 5: Commit** `feat: F19 environment line in keeper/enrich snapshot`

---

### Task 8: 收口

**Files:** `docs/ISSUES.md`、`docs/superpowers/specs/2026-08-31-cluster-assessment.md` §10、`MAINTENANCE.md`

- F17/F19 入 ISSUES §5；F1 丢弃部分标注已随 F17 落地。
- §10：scene_items / environment 运行时已落地、**暂无生产端**（schema 字段已加、生成 prompt 不改）。
- 函数表行号 grep 实测对齐。
- 全量 `python -m pytest tests/ -q`

- [x] **Step 5: Commit** `docs: S3-P3 closeout — F17/F19 ISSUES §5 + §10 + MAINTENANCE`
