【玩家历史行动】
（游戏刚开始）

【世界状态】
已触发事件：（无）
世界标记：（无）


【当前位置】test_room
【场景描述】Unnervingly quiet. Only the occasional buzz of the bulb breaks the silence. A rusty ventilation grate on the north wall has several screws missing.

【可移动方向】
  → car_6：through the iron door passage

【可触发 — AUTO_TRIGGER / INTERACT】
  [AUTO_TRIGGER] id=AT_AMBIENT name="flickering light" 条件=""
  [INTERACT] id=IT1 name="inspect items on table" trigger="inspect the metal table" 条件=""
  [INTERACT] id=IT2 name="read Hawthorne's research log" trigger="sit down and read the log in detail" 条件=""

（无）

【玩家输入】
sang a cheerful song while tapping on the table

实体分为三类：INTERACT（场景交互）、AUTO_TRIGGER（自动触发）、EVENT（全局事件）。
硬性条件（flag/依赖关系）已由系统判定完成。你只需：
1. 判断玩家意图匹配了哪些可触发实体或者其他行为包括(move/search/other)。如有「条件=」字段（软性条件/自然语言描述），评估是否满足，不满足的排除。

返回 JSON：
{
  "actions": [
    {"type": "auto_trigger", "id": "AT1"},
    {"type": "interaction", "id": "I3"},
    {"type": "event", "id": "E22"},
    {"type": "move", "target": "7号车厢"},
    {"type": "search"},
    {"type": "other", "text": "唱了一首歌"}
  ]
}

规则：
- 玩家输入有明确对应的entity优先返回entity结果，之后再考虑search/move/other
- 没有明确指定对象的索、探查、感知行为属于search不触发entity
- 一般来讲玩家一个动作（注意不是一轮输入）只匹配一个结果，但也允许同时匹配多个结果的特殊情况，你可以基于具体文字发挥
- move指移动到别的场景，other泛指所有其他行为
- auto_trigger 必须排在列表最前面
- id 必须从上述实体列表中精确复制
- move：target 填可移动方向中列出的目标
- other：text 用自然语言简述玩家意图
- 排除已完成的交互和已触发的事件
- 如有「条件=」字段，评估是否满足，不满足的排除（硬性条件系统已处理）
- 直接输出 JSON，不要额外文字
