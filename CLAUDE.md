# Project Notes

- 所有 log 文件默认生成在主目录（`C:\Users\micha\PyCharmMiscProject\`）下，除非专门指定，不要放到 `.claude` 目录
- 测试 harness 输出路径：`data/debug/test_harness/<timestamp>/`
- prompt 日志路径：`logs/` 或 test harness 的 `_prompt_log.txt`
