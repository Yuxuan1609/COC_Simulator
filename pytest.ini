[pytest]
testpaths = tests
markers =
    real_llm: 真实 LLM 调用测试（on-demand，默认不运行；pytest -m real_llm 执行）
    real_llm_smoke: real_llm 短烟子集（S1/S2/S3/S4/S20 + escalation；pytest -m real_llm_smoke 执行）
addopts = -m "not real_llm"
